{
  "name": "aos-bridge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "bridge:cli": "npm run build && node dist/cli/bridge-cli.js",
    "bridge:e2e": "npm run build && node dist/cli/bridge-e2e.js",
    "bridge:server": "npm run build && node dist/cli/bridge-server.js",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
