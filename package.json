{
  "name": "aos-bridge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "bridge:e2e": "npm run build && node dist/cli/bridge-e2e.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
